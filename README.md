A dynamic, JavaScript‑powered portfolio website built to showcase my projects, skills, and ongoing progress as a software development student.
This site uses JavaScript objects, arrays, DOM manipulation, and browser storage to generate project content dynamically and create a scalable foundation for future growth.

Features include:

Dynamic project rendering using structured JavaScript objects

sessionStorage persistence with JSON.stringify() / JSON.parse()

Dark Mode with saved user preference

Welcome modal with custom UI

Tooltip‑enhanced contact form

DOM‑generated skills list and dynamic content updates

Clean, responsive layout using HTML5 + CSS3

This portfolio will continue to evolve as I build new projects and expand my development skills.
